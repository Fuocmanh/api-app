{
    "private": true,
    "type": "module",
    "scripts": {
        "composer install": "composer install",
        "key:generate": "php artisan key:generate",
        "refresh db": "php artisan migrate:refresh --seed",
        "start": "php artisan serve"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.0.0"
    }
}
